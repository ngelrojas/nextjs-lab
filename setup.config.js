import '@testing-library/jest-dom/extend-expect';

// Add any additional setup or configuration here
// For example, you can import and configure additional testing libraries

// Example: Configure enzyme for React component testing
// import Enzyme from 'enzyme';
// import Adapter from 'enzyme-adapter-react-16';
// Enzyme.configure({ adapter: new Adapter() });

// Example: Set up global mocks or configurations
// global.fetch = require('jest-fetch-mock');

// You can add any other global setup or configuration that your tests require
